print("Expense Tracker")

total_expense = 0

while True:
    amount = float(input("Enter expense (0 to stop): "))

    if amount == 0:
        break

    total_expense += amount

print("Total Expense: ₹", total_expense)
